# Hugging Face token (only needed if the dataset repo requires authentication)
HF_TOKEN=

# Real LLM API keys (used later, once mock outputs are replaced with real calls)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
