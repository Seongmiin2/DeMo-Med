# DeMo-Med 작업 지시서

대상 저장소: `Seongmiin2/DeMo-Med`
목적: **내부 모순 수치를 제거**하고, 이 저장소가 FAVE-Med의 결정론적 executor 도입 근거임을 명시한다.
작성일: 2026-08-25
예상 소요: 40분

---

## 0. 이 작업의 성격

이 저장소는 연구 무결성 기록이 가장 잘 남아 있는 곳이다. 채점 버그를 스스로 찾아 고치고 **점수가 내려간 것을 그대로 반영**했고, QTc 카드 재실행이 유일했고 **결과를 읽기 전에 했음을 명시**했다.

교수님이 이 기록을 보면 신뢰가 올라간다. 그런데 **본문에 0.70과 0.65가 섞여 있어서** 그 신뢰가 깎일 위험이 있다. 이것만 고치면 된다.

---

## 작업 1. README 수치 모순 수정 【최우선】

### 문제

```
표 + results/demomed_summary.csv   →  demo_med  0.65   ← 정본
README 본문 서술                    →  "0.70 (not 0.75)"  ← 오류
```

`results/demomed_summary.csv` 실제 값:

```csv
method,N,answer_accuracy,no_numeric_output_rate,abstain_rate
llm_only,20,0.35,0.0,0.0
cot,20,0.6,0.0,0.0
open_book,20,0.45,0.0,0.0
demo_med,20,0.65,0.0,0.0
```

### 작업

"A methodology note on the QTc knowledge cards" 절 마지막 문장에서 `0.70 (not 0.75)`를 **`0.65`** 로 수정한다.

수정 후 문장이 논리적으로 맞는지 확인할 것. 재실행으로 DeMo-Med의 Framingham이 정답→오답으로 뒤집혔다는 서술과 최종 0.65가 일관되어야 한다.

### 확인

README 전체에서 다음을 grep으로 점검한다.

```bash
grep -nE "0\.(3[0-9]|4[0-9]|5[0-9]|6[0-9]|7[0-9])" README.md
```

CSV와 불일치하는 값이 더 있는지 전수 확인한다. **CSV가 항상 정본이다.**

---

## 작업 2. FAVE-Med로의 연결 명시 【최우선】

이 저장소의 발견 3(검증 단계가 조용한 산술 오류를 못 잡는다)이 **FAVE-Med에서 결정론적 executor를 도입한 직접 원인**이다. 이 인과가 지금 문서에 없다.

`**Recurring error pattern:**` 문단 **바로 다음**에 추가한다.

```markdown
### 이 발견이 이끈 설계 변경

QTc 사례가 보여준 것은 명확하다.

> 모델이 `arithmetic_check: pass`라고 말하는 것은 실제 산술을 실행했다는
> 뜻이 아니라, 자기 답이 일관적이라고 **주장**한 것이다.
> 단계 분해는 값 누락·단위 오류·조건 불충족은 잡지만,
> 조용한 산술 오류는 잡지 못한다.

따라서 후속 연구에서는 검증 단계를 강화하는 대신
**계산 주체 자체를 바꾸는** 방향을 택했다.

```
DeMo-Med:   LLM이 추출하고 LLM이 계산하고 LLM이 검증
FAVE-Med:   LLM은 추출·판정만, 계산은 결정론적 Python executor
```

이 설계 변경은 [FAVE-Med](https://github.com/Seongmiin2/FAVE-Med)에
exact-ID executor 10종으로 구현되어 있다.
```

---

## 작업 3. 상태 배너 추가 【10분】

README 최상단에 삽입한다.

```markdown
> **저장소 상태: 파일럿 완료 (2026-07), 후속은 FAVE-Med로 통합**
>
> pilot_20 실험은 완료되었으며 아래 수치가 정본입니다.
> 설계된 pilot_100 / 6조건 / 2모델 확장은 **미실행**입니다.
> 통합 프레임워크는 [FAVE-Med](https://github.com/Seongmiin2/FAVE-Med)를 참조하십시오.
```

---

## 작업 4. 미실행 계획 명시 【10분】

`## Pipeline` 섹션의 `make_pilot.py --n 100`과 6조건 확장은 **설계만 되고 실행되지 않았다.** 실행된 것처럼 읽히지 않도록 표기한다.

```markdown
### 실행 상태

| 항목 | 상태 |
|---|---|
| pilot_20 × 4조건 (80 calls) | **실행 완료** |
| 채점 버그 수정 후 재평가 | **실행 완료** |
| QTc 카드 수정 후 부분 재실행 (2행) | **실행 완료** |
| pilot_100 | 미실행 |
| 6조건 (Open-book+abstain, DeMo-single/multi 등) | 코드 존재, 미실행 |
| 2모델 비교 (OpenAI / Together) | 미실행 |
| 임상 검토 | 미실행 |
```

`src/run_experiment.py`가 6조건을 지원하도록 커밋(`f1eb536`)되어 있으므로, **코드는 있으나 실행 결과가 없다**는 구분을 정확히 한다.

---

## 작업 5. 무결성 기록을 눈에 띄게 【5분】

현재 채점 버그 수정과 QTc 재실행 기록이 본문 중간에 묻혀 있다. 교수님이 놓치지 않도록 목차 수준으로 끌어올린다.

`## Real result` 섹션 아래에 한 줄 추가:

```markdown
> 이 표는 **채점 버그 수정 후** 수치입니다. 수정으로 세 방법이 각 1문항씩
> 하락했으며, 불리한 방향의 정정을 그대로 반영했습니다.
> 상세는 아래 "Grading bug fixed after the first pass" 참조.
```

---

## 완료 기준

- [ ] README 본문의 `0.70` → `0.65` 수정
- [ ] grep으로 CSV 불일치 수치 전수 확인 완료
- [ ] FAVE-Med executor 도입 인과가 문서에 명시됨
- [ ] 상태 배너 추가
- [ ] 실행/미실행 구분표 추가
- [ ] 채점 버그 정정 사실이 결과표 근처에 표기됨

---

## 금지 사항

- 무결성 기록(채점 버그·QTc 재실행 서술)을 축약하거나 삭제하지 않는다. **이 저장소의 가장 큰 자산이다**
- 미실행 계획을 실행된 것처럼 표기하지 않는다
- CSV를 README에 맞추지 않는다. 항상 반대 방향으로 수정한다
